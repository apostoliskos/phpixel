---
import { SkipLink } from '@components/odyssey-theme';
import BaseHead from '../components/head/BaseHead.astro';
import type { Props as BaseHeadProps } from '../components/head/BaseHead.astro';
export interface Props {
	seo?: BaseHeadProps;
}

const { seo } = Astro.props;
---

<html lang="el">
	<head>
		<BaseHead {...seo} />

	</head>
	<body>
		<SkipLink />

		<slot name="header" />
		<main id="content">
			<slot />
		</main>
		<slot name="footer" />

		<script>
			const initAnimation = async () => {
				const canvas1 = document.getElementById('network') as HTMLCanvasElement;
				if (canvas1) {
					const { NetworkAnimation } = await import('@utils/NetworkAnimation');
					new NetworkAnimation(canvas1, {});
				}
			};

			// Wait until the browser is idle to start the heavy animation logic
			if ('requestIdleCallback' in window) {
				window.requestIdleCallback(() => initAnimation());
			} else {
				setTimeout(initAnimation, 200); // Fallback for older browsers
			}
		</script>
	</body>
</html>
